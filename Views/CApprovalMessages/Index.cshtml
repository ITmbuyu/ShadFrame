@model IEnumerable<ShadFrame.Models.CApprovalMessages>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CMessages)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CMessages)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CApprovalMessagesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CApprovalMessagesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CApprovalMessagesId })
        </td>
    </tr>
}

</table>
