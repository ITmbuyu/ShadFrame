@model IEnumerable<ShadFrame.Models.ApprovalMessages>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AMessages)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AMessages)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ApprovalMessagesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ApprovalMessagesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ApprovalMessagesId })
        </td>
    </tr>
}

</table>
